# Kopier til supabase/.env (gitignored) for lokal CLI og env()-interpolering i config.toml.
SUPABASE_AUTH_SITE_URL=http://127.0.0.1:3001
# Resend API-nøkkel — påkrevd for config push mot hosted prosjekter (samme som SMTP-passord).
RESEND_API_KEY=
