{
  "name": "eiranova-platform",
  "private": true,
  "packageManager": "pnpm@9.15.1",
  "scripts": {
    "dev:kunde": "pnpm --filter kunde-app dev",
    "dev:nurse": "pnpm --filter nurse-app dev",
    "dev:admin": "pnpm --filter admin-app dev",
    "dev:all": "pnpm --parallel --filter kunde-app --filter nurse-app --filter admin-app dev",
    "generate-cc": "tsx scripts/generate-control-center.ts",
    "queue": "tsx scripts/queue.ts"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
